# OctoFit Frontend

Define `VITE_CODESPACE_NAME` in `.env.local` when running in Codespaces.

Example:

VITE_CODESPACE_NAME=your-codespace-name

When `VITE_CODESPACE_NAME` is unset, the app safely falls back to localhost API URLs.
